# Resume pointer: Task 7: Skill review (fit check against a persona)

## Unit
Unit of the jrit-persona loop run: Task 7 "Skill `review` (fit check against a persona)" from docs/plans/2026-09-15.jrit-persona-plan.md, tracker issue #19.

## State
`skills/review/SKILL.md` and both eval cases (`evals/review-misalignments`, `evals/review-clean`) are written and uncommitted.
Repair pass applied per coordinator review: removed the invented pasted-persona-content exception from SKILL.md (v1 resolves by name via references/resolution.md, full stop), added `case.yaml` plus `scaffold.sh` inside each owned eval case directory to seed a real on-disk `personas/leadership-audience.md` and draft file into the run workspace before the agent starts, and rewrote both `prompt.md` files to reference the persona by name and the draft by workspace-relative path only (no inlined content).
The "report only clearly-evidenced callouts" SKILL.md guidance from the prior pass was kept, since it fixed a real observed judge-flakiness issue and stays within the spec's spirit.

Acceptance check passed this session with `--scaffold` added (required for scaffold_script to run): `claude plugin eval evals/review-misalignments --trust-plugin --no-publish --scaffold --runs 1 --ablation none` and the same for `evals/review-clean`, both all-graders-passing.
A `--keep-temp` run confirmed the resolution path is genuinely exercised: the trace shows a `Read` tool call on the scaffold-seeded workspace path `.../home/cwd/personas/leadership-audience.md`, after `Read` calls on the plugin's own `references/resolution.md` and `references/voice-framework.md`.
Runs were kept at `--runs 1 --ablation none` to conserve quota (an earlier default-flags run this session hit the org's monthly spend limit mid-run); `--scaffold` is required on every invocation of these two cases or the scaffold_script is silently skipped and the persona/draft files will not exist in the workspace.

## Next step
The orchestrator's closing commit (amended on this branch, one commit, byte-verbatim message, no trailers): `git add skills/review evals/review-misalignments evals/review-clean docs/handoffs/2026-09-15.skill-review.resume.md && git commit --amend -m "jrit-persona: review skill with misalignment and clean evals"`.
Any future run of these two cases must pass `--scaffold` (plus `--trust-plugin --no-publish` headless, and `--runs 1 --ablation none` for lean iteration) or the personas/draft files will not be seeded.

## Recent artifacts
- docs/plans/2026-09-15.jrit-persona-plan.md - the plan this unit executed (consumed).
- references/voice-framework.md - the four-axis voice vocabulary the skill applies when judging fit (consumed).
- references/resolution.md - the persona resolution search order and failure contract the skill implements at step 0 (consumed).
- personas/leadership-audience.md - the starter persona both eval cases check drafts against; copied into each run's workspace by that case's scaffold.sh (consumed).
- skills/review/SKILL.md - created; the fit-check skill: resolves a persona by name via resolution.md (no other path in v1), judges the artifact fresh against Stance and Voice, reports concrete located callouts, reports a clean bill when nothing fails, and reports only clearly-evidenced callouts (no padding).
- evals/review-misalignments/fixtures/draft.md - created; a memo with three planted misalignments against leadership-audience (buried summary, banned jargon "leverage", wrong altitude).
- evals/review-misalignments/case.yaml - created; declares `context.scaffold_script: scaffold.sh`.
- evals/review-misalignments/scaffold.sh - created; copies personas/leadership-audience.md and fixtures/draft.md into the run workspace.
- evals/review-misalignments/prompt.md - created; single-turn case referencing the draft by workspace path (`draft.md`) and the persona by name only.
- evals/review-misalignments/graders/three-callouts.md - created; verbatim from the plan.
- evals/review-clean/fixtures/aligned-draft.md - created; a memo already aligned to leadership-audience (executive summary first, ask included in the opening paragraph, no banned jargon, correct altitude), the negative control.
- evals/review-clean/case.yaml - created; declares `context.scaffold_script: scaffold.sh`.
- evals/review-clean/scaffold.sh - created; copies personas/leadership-audience.md and fixtures/aligned-draft.md into the run workspace.
- evals/review-clean/prompt.md - created; single-turn case referencing the draft by workspace path (`aligned-draft.md`) and the persona by name only.
- evals/review-clean/graders/no-false-positives.md - created; verbatim from the plan.
- docs/handoffs/2026-09-15.skill-review.resume.md - this resume pointer (updated).
