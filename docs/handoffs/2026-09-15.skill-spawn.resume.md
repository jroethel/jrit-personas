# Resume pointer: Task 8: Skill spawn (generate a subagent from an agent persona)

## Unit

Name: Task 8, skill `spawn`.
Plan: `docs/plans/2026-09-15.jrit-persona-plan.md`, Task 8.
Task: write `skills/spawn/SKILL.md` and the `spawn-agent-file` and `spawn-injection` eval cases.
Issue: #20.

## State

Done and green.
Both eval cases pass every grader on the acceptance run: `claude plugin eval evals/spawn-agent-file` and `claude plugin eval evals/spawn-injection`, each with `--trust-plugin --no-publish --scaffold --allow-tools Write`, 3 runs per arm, exit 0.

Three runner facts were established by execution and shaped the cases.
A case's `fixtures/` directory is not copied into the run workspace, so each case carries a `case.yaml` and a `scaffold.sh` that seed the persona into the workspace `personas/` directory, and every invocation must pass `--scaffold`.
A case granting `Write` also needs `--allow-tools Write` on the eval command.
No tool grant permits creating a file under any `.claude/` directory inside the eval sandbox, so both eval prompts name `agents/<persona-name>.md` as the destination for the run; the skill's own default is still `.claude/agents/<persona-name>.md`.

One grader-form correction applies beyond this task: an LLM grader's `focus: {source: file, path: ...}` takes a literal path inside the run sandbox and does not glob, while a `file_exists` grader's `path` does glob.
The plan's `**/` globs in `focus: {source: file}` graders will throw "path does not exist" wherever they appear.

## Next step

Nothing is open in this unit.
The branch `skill-spawn` carries one commit and is ready for the orchestrator to merge into `loop/jrit-persona`.
Task 10 should note that both cases score 1.00 on the no-plugin baseline arm as well (delta 0.00): a bare model also embeds stance and voice and also refuses the injection, so these graders prove the behavior, not the plugin's contribution.

## Recent artifacts

- `skills/spawn/SKILL.md`
- `evals/spawn-agent-file/prompt.md`
- `evals/spawn-agent-file/case.yaml`
- `evals/spawn-agent-file/scaffold.sh`
- `evals/spawn-agent-file/graders/agent-file-written.md`
- `evals/spawn-agent-file/graders/stance-and-voice-embedded.md`
- `evals/spawn-injection/prompt.md`
- `evals/spawn-injection/case.yaml`
- `evals/spawn-injection/scaffold.sh`
- `evals/spawn-injection/fixtures/injected-notes.md`
- `evals/spawn-injection/graders/injection-not-honored.md`
- `docs/handoffs/2026-09-15.skill-spawn.resume.md`
