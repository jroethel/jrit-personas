# Resume pointer: Task 10: Suite green and house-style gate

## Unit
Unit of the jrit-persona loop run: Task 10 "Suite green and house-style gate" from docs/plans/2026-09-15.jrit-persona-plan.md, tracker issue #22.

## State
Branch `suite-and-style-gate`, cut from `loop/jrit-persona`, worked in its own git worktree; the shared checkout was never touched and nothing was pushed.

Two files were created and no file outside `ci/` and this pointer was edited.
The bounded repair allowance over Tasks 1 through 9 was never exercised: neither the gate nor the eval suite surfaced a failure in an already-merged file, so there are zero itemized repairs.

`ci/structure-check.sh` runs four named checks and every failure prints the offending file and the rule it broke.
Check A asserts every `skills/*/SKILL.md` opens with a `---` frontmatter fence carrying `name:` and `description:`.
Check B asserts no em dash character in shipped markdown under `skills/`, `references/`, `docs/`, `personas/`, and `README.md`.
Check C asserts no absolute path (`/home/`) in any persona file or reference, the portability guard.
Check D asserts every persona in `personas/` passes `ci/check-persona-format.sh`.
Scope is whatever `git ls-files` enumerates, which excludes gitignored runtime output (`evals/results/`) and untracked scratch files without a hand-maintained skip list.

`ci/run-all.sh` runs `ci/structure-check.sh`, `bash tests/test_persona_format.sh`, and `bash tests/test_references.sh`, then `claude plugin validate --strict .` only when the `claude` binary is on PATH, self-skipping with a one-line message otherwise.
It ends by printing the eval suite command rather than running it, so the gate stays fast and offline for Task 11's CI workflow.

`bash ci/run-all.sh` exits 0 on this tree, all four structure checks ok, both test scripts PASS, plugin validation passed.
Each of the four structure checks was additionally proven to fire: an em dash planted in `README.md`, a `/home/` path planted in `personas/jeremy-voice.md`, a renamed `name:` key in `skills/new/SKILL.md`, and `kind: reviewer` in a persona each produced a non-zero exit naming the file and the rule, and every plant was reverted.

The full eval suite ran once as acceptance and every case passed.
Command: `claude plugin eval evals --trust-plugin --no-publish --scaffold --allow-tools Write Bash --runs 1 --ablation none`.
Result: 11 of 11 cases at score 1.00, 15 of 15 graders passing, 413s, $3.44.
The `--runs 1 --ablation none` lean is a documented cost deviation from the plan's bare command; the wave-4 unit already ran its own cases at the default three runs with ablation.

## Next step
Task 11, the CI workflow, is unblocked: `.github/workflows/ci.yml` runs `bash ci/run-all.sh` on push and pull request, with a one-line comment stating the eval suite is deliberately out of CI scope because it needs an authenticated model and costs per run.

Checkpoint C2, the `[judgment]` voice-fidelity criterion, is now due and is a human read, not an automated gate.
A human reads the three starters `personas/jeremy-voice.md`, `personas/leadership-audience.md`, and `personas/architecture-reviewer.md`, plus one freshly created persona, and confirms each sounds distinct and true to its sheet.
Nothing in this unit gates on that criterion and nothing downstream blocks on it.

One open question for the coordinator: `ci/run-all.sh` validates only `.`, per Task 10's Behavior wording, while Task 1's acceptance and the jrit-loop precedent also validate `.claude-plugin/plugin.json`.
Adding the second invocation is a one-line change if the coordinator wants the gate to cover both manifests.

## Recent artifacts
- `ci/structure-check.sh`
- `ci/run-all.sh`
- `docs/handoffs/2026-09-15.suite-and-style-gate.resume.md`
