# Resume pointer: Task 6: Skill craft (deliverable under voice plus audience)

## Unit
Unit for the jrit-persona loop run: Task 6 "Skill craft (deliverable under voice plus audience)" from docs/plans/2026-09-15.jrit-persona-plan.md, tracker issue #18.

## State
`skills/craft/SKILL.md` is written per the plan's Behavior spec: it resolves both a voice and an audience persona at step 0 via `references/resolution.md`, states both resolved paths, applies the `references/voice-framework.md` transformation matrix to the "written deliverable" row, shapes structure and altitude from the audience persona's Stance, honors `banned-jargon`, and produces the deliverable directly in its response (no file write, no spawned agent, no branded build).
Repair pass complete: seeding moved from inline-Write to scaffold seeding (`case.yaml` plus `scaffold.sh` in each of the two owned case dirs, following the spawn unit's precedent), and `skills/craft/SKILL.md` plus both `prompt.md` files are rewrapped to one full sentence per physical line.
Both `prompt.md` files now name personas only; they no longer inline persona bytes, so the skill must actually resolve and Read `personas/<name>.md` in the run workspace.
`craft-leadership/scaffold.sh` copies `personas/architecture-reviewer.md` and `personas/leadership-audience.md` from the plugin tree (`$(dirname "$0")/../../personas/`) into the run's project `personas/`.
`craft-contrarian/scaffold.sh` copies `personas/architecture-reviewer.md` the same way, plus its own `fixtures/detail-first-audience.md`.
Graders are unchanged (byte-verbatim from the plan) in both cases.

Verified this session:
- Both scaffold scripts run standalone (outside the eval harness) and produce the expected `personas/*.md` files, confirming the `$(dirname "$0")` relative paths resolve correctly from each case's own directory.
- `claude plugin eval evals/craft-leadership --trust-plugin --no-publish --scaffold --allow-tools Write --runs 1 --ablation none`: score 1.00, both graders PASS.
- `claude plugin eval evals/craft-contrarian --trust-plugin --no-publish --scaffold --allow-tools Write --runs 1 --ablation none`: score 1.00, grader PASS.
- One `--keep-temp` pass on craft-leadership: the kept run's trace shows the skill issuing `Read` on both `personas/architecture-reviewer.md` and `personas/leadership-audience.md` under the run's sandboxed working directory, after reading `references/resolution.md` and `references/voice-framework.md` first. This is the evidence the earlier inline-Write seeding could not produce: the skill now demonstrably resolves and reads the persona files rather than working from content already in its context.
- House-style sweep (em dash, absolute path, banned phrase) clean on every file this unit owns, including the two new `case.yaml`/`scaffold.sh` pairs.

Not re-run this pass: the full default-runs (3x per case) plus with/without ablation acceptance sweep, per the coordinator's explicit "keep eval runs lean, quota is tight" instruction. The lean `--runs 1 --ablation none` pass on both cases plus the keep-temp trace evidence is the executed proof for this repair; a fuller sweep can be re-run cheaply later from this same worktree if a suite-wide gate (Task 10) wants it.

## Next step
The orchestrator's closing commit for this unit (amended, not a new commit): `git commit --amend` on the existing single commit, message unchanged byte-verbatim `jrit-persona: craft skill with leadership and contrarian audience evals`, no trailer.
Downstream: Task 9 (cross-cutting resolution evals) depends on Task 5 and Task 7, not Task 6, so nothing is blocked on this unit beyond Task 10's suite-wide gate, which needs every skill task green.

## Recent artifacts
- skills/craft/SKILL.md - created, then rewrapped to one sentence per line; the craft skill behavior spec (resolution, transformation matrix, audience altitude, banned-jargon, direct-response output).
- evals/craft-leadership/prompt.md - rewritten to name personas only, one sentence per line; drives craft with architecture-reviewer as voice, leadership-audience as audience.
- evals/craft-leadership/case.yaml - created; points at scaffold.sh.
- evals/craft-leadership/scaffold.sh - created; seeds the run workspace personas/ from the plugin tree's committed starters.
- evals/craft-leadership/graders/exec-summary-first.md - unchanged, verbatim from the plan.
- evals/craft-leadership/graders/no-banned-jargon.md - unchanged, verbatim from the plan.
- evals/craft-contrarian/prompt.md - rewritten to name personas only, one sentence per line; same deliverable topic, detail-first-audience as the negative-control audience.
- evals/craft-contrarian/case.yaml - created; points at scaffold.sh.
- evals/craft-contrarian/scaffold.sh - created; seeds the run workspace personas/ from the plugin tree's starter plus this case's own fixture.
- evals/craft-contrarian/fixtures/detail-first-audience.md - unchanged; audience persona wanting detail first, no executive summary, `jargon-tolerance: high`.
- evals/craft-contrarian/graders/renders-to-contrarian.md - unchanged, verbatim from the plan.
- docs/handoffs/2026-09-15.skill-craft.resume.md - this resume pointer (updated).
