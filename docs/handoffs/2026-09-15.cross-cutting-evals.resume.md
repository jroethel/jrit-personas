# Resume pointer: Task 9: Cross-cutting evals (resolution, first run, missing name)

## Unit
Unit of the jrit-persona loop run: Task 9 "Cross-cutting evals (resolution, first run, missing name)" from docs/plans/2026-09-15.jrit-persona-plan.md, tracker issue #21.

## State
Branch `cross-cutting-evals`, cut from `loop/jrit-persona`, worked in its own git worktree; the shared checkout was never touched and nothing was pushed.
All three eval cases are written: `evals/resolution-shadow`, `evals/first-run-library`, and `evals/missing-name`, each with `prompt.md`, `case.yaml`, `scaffold.sh`, and its single grader.
Each grader is byte-verbatim from the plan.
Each `prompt.md` carries the acceptance prose in an `expected_outcome: |` frontmatter field and a body that says only what a real user would say.
No file outside the three case directories and this pointer was created or edited; `references/resolution.md` was not touched, because no skill misresolved in any run.

Two runner facts were established by probe in this unit and drive the case design.
The `Write` tool cannot write above the run cwd, so it cannot create the global library under the run's `HOME`: six grant forms were probed (`Write`, `Write(//**)`, `Write(~/**)`, `Write(../**)`, `Write(/**)`, `Write(*)`) and every one was denied.
The `Bash` tool can write above the run cwd, verified on disk in a kept temp directory, so `first-run-library` carries `Bash` in `allowed_tools` and its runs need `--allow-tools Bash`.
No grader was weakened to accommodate this; only the tool grant widened.

Acceptance passed with the plan's command plus the headless flags, `--scaffold`, and `--allow-tools Write Bash`, at the default three runs per case and the default with/without ablation.
All three cases scored 1.00 on every grader in the with-plugin arm, 3 runs each, and the chain exited 0.
The no-plugin baseline arms separate: `first-run-library` scored 0.00 without the plugin (delta +1.00), `missing-name` 0.67 (delta +0.33), and `resolution-shadow` 1.00 (delta 0.00).
The `resolution-shadow` zero delta is expected and not a defect: the scaffold seeds a project `personas/reviewer.md` right where a plain agent would look first, so the baseline arm gets the same answer by accident.
That observation is flagged for Task 10, not fixed here, because the grader criteria are plan-verbatim.

Repair pass after coordinator review: one coaching line was removed from `evals/first-run-library/prompt.md`.
The body said "Set it up if it is missing and then tell me the exact full path of the file you created", which handed the agent the create-if-absent behavior the grader gates on and `references/resolution.md` already owns.
It now reads "Then tell me the exact full path of the file you created", so the skill has to supply that behavior itself.
Re-run after the edit: `first-run-library` score 1.00, `library-created` PASS PASS PASS, at `--runs 1 --ablation none`.
Nothing else changed: the graders, the three scaffolds, and the other two cases were validated clean and were not touched.

## Next step
Make the closing commit on this branch: `git add evals/resolution-shadow evals/first-run-library evals/missing-name docs/handoffs/2026-09-15.cross-cutting-evals.resume.md && git commit -m "jrit-persona: resolution, first-run, and missing-name evals"`, one commit, message byte-verbatim, no trailer of any kind.
Then the orchestrator merges `cross-cutting-evals` into `loop/jrit-persona` and Task 10 can run the full suite.
Any future run of these three cases must pass `--scaffold` or the `scaffold_script` is silently skipped and no persona, library, or draft is seeded; `first-run-library` additionally needs `--allow-tools Bash` (add `Write` when running the whole suite, which other cases need).
Lean iteration form: add `--runs 1 --ablation none`.

## Recent artifacts
- docs/plans/2026-09-15.jrit-persona-plan.md - the plan this unit executed (consumed).
- references/resolution.md - the search order, the state-the-path rule, the first-use library creation rule, and the failure contract that all three cases prove (consumed, unchanged).
- skills/review/SKILL.md - the consumer under test in `resolution-shadow` and `missing-name` (consumed, unchanged).
- skills/new/SKILL.md - the consumer under test in `first-run-library` (consumed, unchanged).
- evals/resolution-shadow/case.yaml - created; declares `context.scaffold_script: scaffold.sh`.
- evals/resolution-shadow/scaffold.sh - created; writes two different personas both named `reviewer`, one into the run `HOME`'s global library and one into the project `personas/` directory, with visibly different Stance role lines, plus a short draft to review.
- evals/resolution-shadow/prompt.md - created; asks `review` to check the seeded draft against `reviewer` and to say which persona file it used.
- evals/resolution-shadow/graders/project-wins.md - created; verbatim from the plan.
- evals/first-run-library/case.yaml - created; declares `context.scaffold_script: scaffold.sh`.
- evals/first-run-library/scaffold.sh - created; seeds nothing and instead asserts the first-run precondition, that the run `HOME` has no `.config/jrit/personas/` yet.
- evals/first-run-library/prompt.md - created; a fully-stated persona description asking `new` to keep it in the personal library on a machine that has none yet and to state the full path it created; `allowed_tools` includes `Bash` for the reason in State.
- evals/first-run-library/graders/library-created.md - created; verbatim from the plan.
- evals/missing-name/case.yaml - created; declares `context.scaffold_script: scaffold.sh`.
- evals/missing-name/scaffold.sh - created; seeds an empty global library in the run `HOME`, asserts no project `personas/` directory exists, and writes the draft to review.
- evals/missing-name/prompt.md - created; asks `review` to use a persona name that exists in neither location.
- evals/missing-name/graders/clear-failure.md - created; verbatim from the plan.
- docs/handoffs/2026-09-15.cross-cutting-evals.resume.md - this resume pointer (created).
